using UnityEngine;

public class SoundsList : MonoBehaviour
{
    public enum InteractionSoundType
    {
        Click
    }
    public enum InteractionSoundOn
    {
        PointerUp,
        PointerDown
    }
}
